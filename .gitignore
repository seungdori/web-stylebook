.DS_Store
Thumbs.db

# Local temp/test files
*.tmp
*.temp
test_copy.html

# Node logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
node_modules/
dist/
.vite/

# Maintainer-only local notes
MAINTAINER_DOMAIN_NOTES.md
readme_product.md

# Internal dev scripts (not for publish)
PLAN.md
add-nav.mjs
fix_globals.js

# Playwright / MCP browser artifacts
.playwright-cli/
.playwright-mcp/
output/playwright/

# Paid Pro Kit source — never commit. Teaser version lives in
# src/data/proKitSamples.ts. Full kits are zipped from here for Gumroad.
products/

# Maintainer-only Pro Kit playbook (pricing, hide mechanism, launch steps).
readme_product.md

# Working screenshots at repo root (before/after comparisons, scratch captures).
# Production assets live under src/ or public/ — keep root clean.
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp

# Local env — never commit secrets (checkout URLs, keys)
.env
.env.*
!.env.example

# Throwaway render harness (contains copies of paid products/)
.kit-harness/
