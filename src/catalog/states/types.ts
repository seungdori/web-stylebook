// State Atlas types — re-exported from the canonical catalog type contract.
export type {
  StateSurface, StateRecipe, StateAccessibility, StateMotionGuidance,
  StateCategory, StateCriticality,
} from '../types';
