{
  "schema": "webstylebook.quality-budgets.v1",
  "baseline": "quality-baseline-2026-09-22.json",
  "policy": "Reviewed implementation budgets. Changes require a before/after report and a concrete fidelity or compatibility justification. Timing is reported without a hard CI threshold because shared-host scheduling and platform differences make sub-millisecond comparisons noisy.",
  "budgets": [
    { "metric": "entryJsGzipBytes", "scope": "site", "max": 160000, "unit": "bytes", "reason": "Reviewed after measuring 141,663 B versus baseline79,875 B. The synchronous validated workspace/shared visual resolver intentionally moves code into the entry. Whole route module closures below prevent chunk partitioning from hiding actual growth; this is an initial JS cost, not a performance gain." },
    { "metric": "homeJsClosureGzipBytes", "scope": "site", "max": 250000, "unit": "bytes", "reason": "Baseline149,051 B; first complete implementation224,336 B (+50.5%). Reviewed cost of functional specimen, semantic contracts, local resolution and persistent validated state. Excludes CSS/font/media transfer, which needs browser measurement." },
    { "metric": "colorSystemJsClosureGzipBytes", "scope": "site", "max": 210000, "unit": "bytes", "reason": "Baseline110,546 B; first implementation179,918 B. Reviewed absolute ceiling for complete local semantic editing, validation, specimen and export; includes entry and static dependencies." },
    { "metric": "typographyJsClosureGzipBytes", "scope": "site", "max": 210000, "unit": "bytes", "reason": "New editor measured184,893 B; no equivalent baseline editor. Includes full role editing, font metadata/fallback state, specimen and export." },
    { "metric": "compareJsClosureGzipBytes", "scope": "site", "max": 210000, "unit": "bytes", "reason": "Baseline108,727 B; first implementation180,485 B. Reviewed absolute ceiling for shared-content and axis comparison with preserved draft state." },
    { "metric": "promptWorkflowJsClosureGzipBytes", "scope": "site", "max": 210000, "unit": "bytes", "reason": "Baseline152,748 B; first implementation186,025 B. Reviewed ceiling for exact draft handoff and locked-scope refinement, not all-reference content." },
    { "metric": "allJSGzipBytes", "scope": "site", "max": 1150000, "unit": "bytes", "reason": "Baseline 944,130 B across all lazy routes. Allows complete visual contracts and editor behavior; catches accidental corpus or duplicated dependencies." },
    { "metric": "allCSSGzipBytes", "scope": "site", "max": 260000, "unit": "bytes", "reason": "Baseline 241,230 B across all lazy routes. Small allowance for shared specimens/editor controls; does not authorize a second gallery stylesheet." },
    { "metric": "globalFontFamilyRequests", "scope": "site", "max": 0, "unit": "count", "reason": "Baseline global CSS requested 27 families. Font loading now belongs to the active shell/specimen loader; no all-catalog global import may return. Actual browser font requests are checked separately." },
    { "metric": "selectedStaticMaxBytes", "scope": "site", "max": 32000, "unit": "bytes", "reason": "New selected handoff includes exact role typography, provenance and honest checks; 32 KB is a bounded one-style payload, independently guarded against unrelated catalog/locales." },
    { "metric": "selectedPromptMaxBytes", "scope": "site", "max": 34000, "unit": "bytes", "reason": "The same one-style static handoff plus a bounded implementation preface. Measures every style and content locale with empty user project context; user-authored project text is additional explicit input." },
    { "metric": "mcpSelectedMaxBytes", "scope": "mcp", "max": 48000, "unit": "bytes", "reason": "Reviewed after the first complete implementation measured 49,549 B maximum with pretty JSON duplicated inside rendered. Compact rendered JSON preserves all exact fields. The 48 KB separate MCP cap covers structured visualContract, machine-readable rendered output, and backward-compatible token aliases. Website selected artifacts retain 32 KB; unrelated catalog content remains forbidden." },
    { "metric": "mcpWireMaxBytes", "scope": "mcp", "max": 76000, "unit": "bytes", "reason": "Final JSON CallToolResult maximum measured72,488 B after lossless source/repair metadata was retained in text fallback. Reviewed76 KB ceiling includes structuredContent and fallback, excluding only the small JSON-RPC wrapper. The prior72 KB draft estimate was488 B short; no metadata was dropped to conceal the cost." },
    { "metric": "mcpTextMaxBytes", "scope": "mcp", "max": 24000, "unit": "bytes", "reason": "Actual text fallback maximum measured 20,572 B. Keeps one resolved visual contract and concise notes bounded without dropping provenance, repairs or field origins. Client consumption determines actual model context." },
    { "metric": "mcpAllFormatsWireMaxBytes", "scope": "mcp", "max": 112000, "unit": "bytes", "reason": "Final maximum108,560 B across48 styles ×3 locales ×4 formats (576 in-memory tool calls). Tailwind/theme output includes role styles and a lossless metadata companion; CSS text-only clients also receive the current edited companion. Reviewed separately from JSON to avoid removing required fields." },
    { "metric": "mcpAllFormatsTextMaxBytes", "scope": "mcp", "max": 40000, "unit": "bytes", "reason": "Final text maximum38,798 B across576 format cases. This includes code plus current source/repair/typography metadata where the code format cannot represent it; no full catalog or external reference corpus is included." },
    { "metric": "mcpCatalogBytes", "scope": "mcp", "max": 5500000, "unit": "bytes", "reason": "Baseline 3,700,371 B. Allows explicit source-grounded contracts for all 48 styles; a full catalog is a distribution artifact, not a selected response." },
    { "metric": "mcpPackageBytes", "scope": "mcp", "max": 1200000, "unit": "bytes", "reason": "Baseline npm tarball 920,576 B. Allows compressible authored contracts and shared resolver; no runtime browser or model dependencies." },
    { "metric": "mcpPackageUnpackedBytes", "scope": "mcp", "max": 6400000, "unit": "bytes", "reason": "Baseline 4,273,435 B. Allows the complete catalog and shared resolution/export source; package contents remain inspectable through npm pack dry run." }
  ]
}
